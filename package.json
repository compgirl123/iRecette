{
  "name": "iRecette",
  "version": "2.1.0",
  "private": true,
  "description": "iRecette: Find the perfect recipe for what you want to cook!",
  "main": "index.js",
  "author": "iRecette",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp _redirects build/",
    "format": "prettier --write 'src/**/*.js' && eslint --fix 'src/**/*.js'"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "5.2.0",
    "react-scripts": "^5.0.1",
    "sanitize.css": "12.0.1"
  },
  "devDependencies": {
    "prettier": "2.1.1",
    "semantic-ui-css": "2.5.0",
    "semantic-ui-react": "2.1.4"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
